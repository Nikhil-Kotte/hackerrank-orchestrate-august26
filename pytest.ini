[pytest]
# The default suite is hermetic: no network, no API key, byte-identical output.
# Live adapter contract tests are opt-in with `python -m pytest -m live`.
addopts = -m "not live"
markers =
    live: calls the real OpenRouter or Groq API (deselected by default)
